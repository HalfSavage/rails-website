source 'https://rubygems.org'

gem 'devise', '>= 3.2.0'
gem 'rails', '>= 4.1.6'
gem 'pg'
gem 'uglifier', '>= 1.3.0'
gem 'coffee-rails', '~> 4.0.0'
gem 'therubyracer', platforms: :ruby
gem 'jquery-rails'
gem 'jbuilder'
gem 'schema_plus' # Adds view support and advanced index support to migrations and schema dumps
gem 'will_paginate', '~> 3.0'
gem 'will_paginate-bootstrap'
gem 'composite_primary_keys', '~> 7.0.2'
gem 'less-rails'

group :development do
  gem 'spring'
  gem 'quiet_assets' # Suppresses all the noisy blabbering in the console when running local server for development
end

group :test, :development do
  gem 'marky_markov' # Used in seeds.rb to generate thread and message content
  gem 'pry'
  gem 'pry-rails'
  gem 'pry-byebug'
  gem 'better_errors'
end
