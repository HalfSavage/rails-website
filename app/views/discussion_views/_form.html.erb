<%= form_for(@discussion_view) do |f| %>
  <% if @discussion_view.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@discussion_view.errors.count, "error") %> prohibited this discussion_view from being saved:</h2>

      <ul>
      <% @discussion_view.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :post_id %><br>
    <%= f.text_field :post_id %>
  </div>
  <div class="field">
    <%= f.label :member_id %><br>
    <%= f.text_field :member_id %>
  </div>
  <div class="field">
    <%= f.label :tally %><br>
    <%= f.number_field :tally %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
