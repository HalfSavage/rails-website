<%= form_for(@profile_view) do |f| %>
  <% if @profile_view.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@profile_view.errors.count, "error") %> prohibited this profile_view from being saved:</h2>

      <ul>
      <% @profile_view.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :member_id %><br>
    <%= f.text_field :member_id %>
  </div>
  <div class="field">
    <%= f.label :member_id %><br>
    <%= f.text_field :member_id %>
  </div>
  <div class="field">
    <%= f.label :tally %><br>
    <%= f.number_field :tally %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
